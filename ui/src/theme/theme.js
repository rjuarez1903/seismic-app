import createTheme from '@mui/material/styles/createTheme';

const theme = createTheme({
    palette: {
        primary: {
            main: '#CB6242'
        },
        secondary: {
            main: '#457B9D'
        },
    }
});

export default theme;
